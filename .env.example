# Conexão do Postgres (api + alembic). Copie para .env e ajuste.
DATABASE_URL=postgresql+psycopg2://fifa:fifa@localhost:5432/fifa

# Credenciais usadas pelo serviço db do docker-compose.
POSTGRES_USER=fifa
POSTGRES_PASSWORD=fifa
POSTGRES_DB=fifa
