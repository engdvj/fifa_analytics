import pandas as pd


def normalize_team_stats(records: list[dict]) -> pd.DataFrame:
    return pd.DataFrame(records)


def normalize_player_stats(records: list[dict]) -> pd.DataFrame:
    return pd.DataFrame(records)

